# 能力文档规则

每个已调模块或算法必须有独立能力文档。推荐文件名：

```text
<CAPABILITY_NAME>.md
```

必填章节：用途、状态、稳定接口、输入输出、配置、所有权/实时性、故障行为、最小示例、接入步骤、
验证证据、性能、已知限制、版本/哈希和回退方法。缺少这些内容的模块不能登记为 `reusable`。

模块修复或调优后，先运行原回归，再更新能力文档；比赛 Mission 只通过公开接口使用模块。
