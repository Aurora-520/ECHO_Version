# 2026-07-15 V0 正式工程骨架

性质：normal

## 目标与范围

- 将唯一正式视觉工程固定为 `E:\ECHO_Version`。
- 完成只读资料调查后的 V0 工程规则、可运行底座、协议草案和交接机制。
- 不实现具体赛题，不连接硬件，不刷系统，不修改任何只读参考来源。

## 开始状态

- `E:\ECHO_Version` 只有空 `.git` 和 `.agents` 目录。
- 空 `.git` 不是有效仓库，没有源码、文档、提交、远端或 dirty 用户文件。
- 用户确认可以初始化 Git，默认分支为 `main`，不配置远端。
- `E:\ECHO` 当时存在用户 dirty 文件，只读记录后未触碰。

## 调查

- 阅读 ECHO 的 AGENTS、PROJECT_STATUS、CURRENT_WORKFLOW、架构边界、工程红线、Phase 1F 和
  worklog 规则。
- 盘点 6 张往年赛题截图，形成循迹、瞄准、光斑、平面标定、棋盘、泊车和运动目标能力表。
- 审查 `E:\version` 全部 8 个 Python 文件，重点逐段阅读 1725 行的
  `version 4.4.0(1).py`。
- 审查 `E:\电赛开源` 的电控框架、云台视觉接收/卡尔曼和逐飞相机示例。
- 未找到 `model_262872.mud`、`model_271670.mud`、标签、训练数据或说明。

## 修改

- 建立 AGENTS、README、Git 忽略/属性、状态、交接、工作流、架构、硬件、协议、风险、指标、
  Phase、学习路线和 worklog。
- 建立 `src/echo_vision` 分层包、配置、核心类型、端口、适配器、服务、Pipeline、runtime 和协议。
- 建立 configs、models、datasets、recordings、calibrations、protocol、tools 和 tests。
- 设备 CLI 在 V0 只输出操作计划；系统烧录不进入普通 CLI。

## 关键决定

- 任何 `E:\ECHO-Vision` 均视为旧笔误，唯一正式路径为 `E:\ECHO_Version`。
- MaixCAM Pro 暂定主平台，树莓派 4B 为开发/采集/备用，A/B 测试前不冻结。
- MaixCAM Pro 不假设内置陀螺仪，外接 IMU 以后通过可选端口接入。
- 相机和 UART 各自单一所有者；视觉不控制执行器，MCU 是唯一最终输出者。
- V0 文件源使用 NumPy/Pillow；OpenCV、Maix 和 pyserial 均延迟导入。
- no-op Pipeline 输出 `valid=false`，不伪造目标坐标。

## 硬件状态

- MaixCAM Pro 设备在手，但本次没有连接或读取系统版本。
- 树莓派系统、独立相机型号和 USB-TTL 型号未知。
- UART 未接线，未连接电机、云台或整车动作输出。
- `discover_devices.ps1` 在当前执行环境未发现 USB 网卡或串口候选。

## 验证

- Python 3.12.13、NumPy 2.3.5、Pillow 12.2.0。
- `python -m unittest discover -s tests -v`：14 tests，全部通过。
- 配置/schema：5 个 JSON 文件解析通过。
- 包导入：26 个 `echo_vision` 模块导入通过。
- `tools/device_cli.py discover`：正确输出 `deferred` 操作计划，不执行远程动作。
- `tools/discover_devices.ps1`：脚本执行成功，当前候选列表为空。
- CLI 回放两张只读赛题截图：2 帧、0 failure，输出
  `artifacts/v0-smoke/results.jsonl`，该路径被 Git 忽略。
- 回放摘要为约 31 ms 总时间和约 64.5 FPS，但这是 no-op 数据流烟雾测试，不是视觉算法性能。
- 当前环境没有 OpenCV、pytest、PyYAML 和 pyserial；对应测试未执行。

## 问题与判断

- 首轮测试发现配置默认 tuple 读取错误和极短回放 0 ms 计时问题，修复后通过。
- 首次 CLI 输出到 `C:\tmp` 因当前 Python 沙箱权限失败，回放器原先只计 failure 不显示原因；
  已增加 `failure_messages`，并改用 workspace ignored artifacts 完成验证。
- 旧代码的算法思路可逐个提炼，但单文件结构、文本串口、硬编码和隐藏状态不得继承。

## 风险与下一步

- V1 首先连接 MaixCAM Pro 并只读发现系统、网络、存储、相机、温度和 Maix API。
- 只读检查设备上的旧模型路径，仍不得猜标签。
- 确认相机型号和接口后建立真实采集；同时建立第一批固定图片数据集。
- OpenCV、MaixCAM、树莓派相机、UART 和整机验证仍为未执行。

## 结束状态

- 分支：`main`
- 阶段提交：本文件所在 V0 基线提交
- annotated tag：`vision-v0-baseline`
- 远端/push：无远端，未 push
- 用户数据、stash、备份：未发现、未修改、未删除
