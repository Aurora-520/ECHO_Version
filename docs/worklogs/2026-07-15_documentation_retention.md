# 2026-07-15 文档传承与模块复用约束

## 目标与范围

把用户新增的长期约束写入正式工程：每阶段保存完整调试复盘，为无视觉基础队员建立学习日志，
为已调模块建立安全快速复用说明，并强制新任务/新聊天先读取这些记录。

## 开始状态

- V0 代码基线已在 `71d827a` / `vision-v0-baseline` 完成纯软件验收。
- 已有 AGENTS、状态、交接、Phase 和 worklog，但知识分类与复用门禁不完整。
- MaixCAM、树莓派相机、OpenCV、UART 和整机测试均未执行。

## 调查与修改

- 新增 `docs/DOCUMENTATION_SYSTEM.md`，定义五类文档及强制开工/收工流程。
- 新增 `docs/debuglogs`，记录问题的现象、证据、根因、修复、验证和预防。
- 新增 `docs/learning` 的 V0 基础学习材料，面向没有视觉基础的队员。
- 新增能力目录与 V0 运行底座复用说明，规定模块状态和升级门禁。
- 更新 AGENTS、当前交接、工作流、状态、README、V0 Phase 和 worklog 规则。

## 硬件状态

本次不连接、不部署、不刷写任何硬件。MaixCAM、树莓派相机、UART 和整机仍为 `deferred`。

## 验证

- 检查所有新文档能从常驻入口找到。
- 检查开工顺序包含状态、交接、文档体系、Phase、能力、调试、学习和 Git 状态。
- 检查阶段完成与模块 `reusable` 均有不可跳过的门禁。
- `git diff --check` 通过。
- PowerShell 默认 `python` 实际为 Python 3.6.5；已记录为环境问题，不作为代码失败。
- 使用 Codex bundled Python 3.12.13 和 `PYTHONPATH=src` 复测：14/14 单元测试通过。
- 本次没有修改运行时代码；原 V0 2 帧回放结论不因文档修改而升级或失效。

## 问题与判断

原 worklog 能回答“做了什么”，但不能稳定承担问题证据、初学者教学和模块 API 三种长期职责。
因此采用分目录记录和统一索引，而不是继续扩大单个 worklog。

## 风险与下一步

- 新规则只有在每个后续任务真实执行时才有价值，不能只创建空模板。
- V1 设备发现和 V2 首批矩形/圆算法必须分别产生调试、学习和能力记录。
- 硬件性能、温度、UART 错误和整机行为仍需真实设备证据。

## 结束状态

文档传承体系已形成第一版，后续聊天可从仓库内恢复事实、问题、学习路径和模块复用方法。
